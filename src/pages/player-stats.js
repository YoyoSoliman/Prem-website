import React from "react";

function PlayerStats({ playerName }) {
    return (
        <div>
            <h2>Player Stats for {playerName}</h2>
            {/* Render player stats data here */}
        </div>
    );
}

export default PlayerStats;